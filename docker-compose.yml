services:
  app:
    container_name: my-profile-front
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    environment:
      - WATCHPACK_POLLING=true
    tty: true
    stdin_open: true
